application.args=
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/Scout.jar
dist.javadoc.dir=${dist.dir}/javadoc
file.reference.avetanaBluetooth.jar=externLibs/avetanaBluetooth.jar
file.reference.BlueCoveJSR82-Patched-By-Benhui-net.jar=externLibs/BlueCoveJSR82-Patched-By-Benhui-net.jar
file.reference.NetUtil.jar=externLibs/NetUtil.jar
file.reference.Scout-src=src
file.reference.swing-layout-1.0.jar=externLibs/swing-layout-1.0.jar
jar.compress=false
javac.classpath=\
    ${file.reference.NetUtil.jar}:\
    ${file.reference.avetanaBluetooth.jar}:\
    ${file.reference.swing-layout-1.0.jar}:\
    ${libs.swing-layout.classpath}:\
    ${file.reference.BlueCoveJSR82-Patched-By-Benhui-net.jar}
# Space-separated list of extra javac options
javac.compilerargs=-Xlint:unchecked -Xlint:deprecation
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.class=citar.diablu.bc.DiABluBC
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.dir=${file.reference.Scout-src}
